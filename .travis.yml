language: 
  php

php: 
  - 5.5

env:
  global:
    - NODE_VERSION="4"
    - NODE_JS_VERSION="4"
    - TRAVIS_NODE_VERSION="4"

addons:
  hosts:
    - localhost

dist: trusty
sudo: required

branches:
  only:
  - travis_ci

before_install:
  - uname -a
  - "/sbin/start-stop-daemon --start --quiet --pidfile /tmp/custom_xvfb_99.pid --make-pidfile --background --exec /usr/bin/Xvfb -- :99 -ac -screen 0 1280x1024x16"
  - nvm install node

install:
  - npm install -g brunch@2.9.1
  - npm install -g bower
  - npm install
  - bower update
  - sleep 1

before_script:
  - "export PATH=/usr/local/phantomjs-2.0.0/bin:$PATH"
  - export HOSTNAME=localhost
  - cd www
  - "echo $(pwd) && ls"
  - sudo apt-get update
  - sudo apt-get install -y php5 php5-mcrypt php5-curl php5-cli php5-gd -qq
  - sudo apt-get install -y imagemagick php5-imagick -qq
  # configure apache virtual hosts
  - sudo cp -f ../build/travis-ci-apache /etc/apache2/sites-available/000-default.conf
  - sudo sed -e "s?%TRAVIS_BUILD_DIR%?$(pwd)?g" --in-place /etc/apache2/sites-available/000-default.conf
  - sudo sed -e "s?Listen 80?Listen 8080?g" --in-place /etc/apache2/ports.conf
  - echo "ServerName localhost" | sudo tee -a /etc/apache2/apache2.conf
  # permissions and groups
  - sudo usermod -a -G www-data travis
  - sudo chown -R www-data:www-data /home/travis/build/sc0ttj/Project/www
  - chmod go-rwx /home/travis/build/sc0ttj/Project/www
  - chmod go+x /home/travis/build/sc0ttj/Project/www
  - chgrp -R www-data /home/travis/build/sc0ttj/Project/www
  - chmod -R go-rwx /home/travis/build/sc0ttj/Project/www
  - chmod -R g+rx /home/travis/build/sc0ttj/Project/www
  - chmod -R g+rwx /home/travis/build/sc0ttj/Project/www
  - ls -la /home/travis/build/sc0ttj/Project/www
  - sudo a2enmod rewrite
  - sudo service apache2 restart
  # log useful debugging info
  - sudo ls -R /etc/apache2/
  - sudo cat /etc/apache2/apache2.conf
  - sudo cat /etc/apache2/sites-available/000-default.conf
  - sudo cat /etc/apache2/ports.conf
  - sudo apachectl fullstatus | tail -1
  - sudo netstat -anp | grep apache2

  # back to root dir, we will run test from there
  - cd ..
  - sleep 1
  - BUILD=with-tests brunch b
  - sudo chown -R www-data:www-data /home/travis/build/sc0ttj/Project/www

script: phantomjs src/test/js/_phantom_runner.js

after_script:
  - sudo cat /var/log/apache2/error.log
  - sudo cat /var/log/apache2/access.log
  - ls /home/travis/build/sc0ttj/Project/www
  - ls -R /home/travis/build/sc0ttj/Project/www/demo/
