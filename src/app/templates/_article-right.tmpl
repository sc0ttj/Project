  <div class="article article-right" data-name="article-right">
    
    <picture>
      <img class="article-right-img" src="{{imagePlaceholder}}" alt="" />
    </picture>
    
    <h2>{{article.heading}}</h2>
    
    <div class="cms-editable-region">
      <p>{{article.para}}</p>
      <p>{{article.para}}</p>
      <p>{{article.para}}</p>
    </div>

  </div>
